[workspace]

members = [
    "alg-list-gen",
    "bld-solver",
    "cube",
    "cycles",
    "kociemba",
    "random-state",
]

[workspace.dependencies]
rand = { version = "0.8", default-features = false }
